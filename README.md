Messing with React native reanimated, because it's interesting. 

To run:


```npm install -g expo-cli```

Git clone this repo 

cd this repo 

npm install 

npm start 

for the full effect: 

Listen to Mothership by Led Zepplin, I always seem to when stumbling my way through animations + transformations etc 
